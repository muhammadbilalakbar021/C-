// Use `std::setw(8)` and `std::left` to print the name, sport, height, and weight values of the athlete
// Use `std::setw(10)` to print the style, color, and size of its associated `AthleticShoe` object
